import { createReducer, combineReducers } from '@reduxjs/toolkit';
import { SHOW_DIALOG, HIDE_DIALOG, TOGGLE_DIALOG, CHANGE_LOCALE } from '../Actions/ActionTypes';

export const appReducer = combineReducers({

});

