// eslint no-extend-native: ["error", { "exceptions": ["String"] }]

import { capitalize, splitWords } from './StringUtils';

export default function() {
//    String.prototype.capitalize = capitalize;
//    String.prototype.splitWords = splitWords;
}